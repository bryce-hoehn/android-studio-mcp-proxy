services:
  android-studio-mcp-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: android-studio-mcp-proxy
    ports:
      - "5555:5555"
    volumes:
      - ./mcp_settings.json:/app/config/mcp_settings.json:ro,z
    restart: unless-stopped